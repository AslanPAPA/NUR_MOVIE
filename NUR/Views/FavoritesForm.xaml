<UserControl
    x:Class="NUR.Views.FavoritesForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid Background="#1A1A1B">

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FavoriteMovies}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Width="130"
                            Margin="10"
                            Background="#222"
                            CornerRadius="8"
                            Cursor="Hand"
                            MouseDown="MovieCard_MouseDown">

                            <StackPanel>

                                <Image
                                    Height="190"
                                    Source="{Binding Poster}"
                                    Stretch="UniformToFill" />

                                <TextBlock
                                    Margin="5"
                                    FontWeight="Bold"
                                    Foreground="White"
                                    Text="{Binding Title}"
                                    TextWrapping="Wrap"/>

                                <TextBlock
                                    Margin="5,0,5,5"
                                    Foreground="Gray"
                                    Text="{Binding Year}" />

                            </StackPanel>

                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>
        </ScrollViewer>

    </Grid>
</UserControl>